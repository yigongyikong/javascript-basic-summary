/**
 * 10 + 20은 연산을 통해 새로운 값 30을 생성한다.
 * 생성된 값 30은 result라는 변수에 저장할 수 있다.
 * 이처럼 변수에 값을 저장하는 것을 할당(assignment)이라 하고,
 *  변수에 저장된 값을 읽어 들이는 것을 참조(reference)라 한다.
 * 
 * 사람이 이해할 수 있는 언어로 명명한 변수 이름을 통해
 *  변수에 저장된 값의 의미를 명확히 할 수 있다.
 */

var result = 10 + 20;